import { Client, ClientOptions } from 'graphql-ws';
export interface RestartableClient extends Client {
    restart(): void;
}
export declare const createRestartableClient: (options: ClientOptions) => RestartableClient;
